---
description: This command is to be used to run a linting and typing check on the frontend repository.
---

These commands are to be run in the frontend repository

Run a lint check first.

Use this command
`npm run lint`

Then fix all linting errors.

Then run the typescript compiler.
`npx tsc --noEmit`

Then fix all typing errors.