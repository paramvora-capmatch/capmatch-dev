# ============================================================================
# Scheduled Jobs - Environment Variables Template
# ============================================================================
# Copy this file to each job directory as .env and fill in your actual values
# ============================================================================

# ----------------------------------------------------------------------------
# Common Variables (Required for ALL jobs)
# ----------------------------------------------------------------------------

# Supabase Configuration
SUPABASE_URL=http://127.0.0.1:54321                    # Local dev or production URL
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here   # Get from Supabase dashboard

# Logging Configuration
LOG_LEVEL=INFO                                          # DEBUG, INFO, WARNING, ERROR

# ----------------------------------------------------------------------------
# Job-Specific Variables
# ----------------------------------------------------------------------------

# For: renew-calendar-watches
# Google OAuth credentials for calendar integration
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# For: unread-thread-nudges
# Threshold in minutes for considering a message "stale" (default: 180 = 3 hours)
THRESHOLD_MINUTES=180

# For: meeting-reminders, resume-incomplete-nudges
# No additional job-specific variables needed

# ----------------------------------------------------------------------------
# Dry Run Mode (Optional - for meeting-reminders, unread-thread-nudges)
# ----------------------------------------------------------------------------
DRY_RUN=false                                           # Set to true for testing

# ============================================================================
# Production Deployment Notes
# ============================================================================
# For production VM deployment:
# 1. Create separate .env files in each job directory
# 2. Use production Supabase URL and service role key
# 3. Set DRY_RUN=false
# 4. Protect .env files with proper permissions (chmod 600)
# 5. Never commit .env files to git (already in .gitignore)
# ============================================================================
